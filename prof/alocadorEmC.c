#include <stdio.h>
#include <stdlib.h>

void iniciaAlocador();

int main(int argc, char const *argv[])
{
   printf("\n");
   iniciaAlocador();
   // imprimeBrk();
   // x=meuMalloc(100);
   // imprimeBrk();
   // y=meuMalloc(900);
   // imprimeBrk();
   // meufree(x);
   // imprimeBrk();
   // meufree(y);
   // imprimeBrk();
}

